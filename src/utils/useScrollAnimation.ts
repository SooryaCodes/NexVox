// This file is intentionally left empty as we're migrating to AOS for animations
// The file is kept to prevent import errors during migration
// After migration is complete, references to this file should be removed

export const useScrollAnimation = () => {
  return [null, false];
};

export const getAnimationStyles = () => {
  return {};
};

export const getAnimationClasses = () => {
  return '';
}; 